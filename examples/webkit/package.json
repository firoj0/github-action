{
  "name": "example-webkit",
  "version": "1.1.0",
  "description": "running Cypress using the experimental webkit",
  "main": "index.js",
  "scripts": {
    "test": "npm run cy:run",
    "info": "cypress info",
    "cy:run": "cypress run",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:edge": "cypress run --browser edge",
    "cy:run:firefox": "cypress run --browser firefox",
    "cy:run:webkit": "cypress run --browser webkit"
  },
  "private": true,
  "devDependencies": {
    "cypress": "13.0.0",
    "image-size": "^1.0.2",
    "playwright-webkit": "1.34"
  }
}
